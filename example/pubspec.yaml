name: app_string_example
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.1

dependencies:
  app_strings:
    path: ../
  easy_localization: ^3.0.7+1
  flutter:
    sdk: flutter

dependency_overrides:
  path: ^1.9.1
  meta: ^1.17.0

  

dev_dependencies:
  build_runner: ^2.6.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
